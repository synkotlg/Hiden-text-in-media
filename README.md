# Hiden-text-in-media
Hide encrypted text inside any media or binary file (video, audio, archive, etc.) in a simple, cross-platform and reversible way.  This project appends encrypted data at the end of a file, without breaking its original usability.
